import 'package:flutter/material.dart';

class AreaCircle extends StatelessWidget {
  const AreaCircle({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
